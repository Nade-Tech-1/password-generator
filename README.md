Password Generator 🔐

A simple Python password generator built with Python.

Features

- Allows the user to choose a password length
- Accepts passwords between 8 and 64 characters
- Generates passwords using letters, numbers, and punctuation
- Uses Python's "random" and "string" modules

What I Learned

- Using loops in Python
- Working with strings
- Using "random.choice()"
- Using conditional statements
- Handling user input